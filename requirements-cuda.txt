# Common dependencies
-r requirements-common.txt

# Dependencies for NVIDIA GPUs
ray >= 2.9
nvidia-ml-py >= 12.560.30 # for pynvml package
torch; platform_machine != 'aarch64'
# These must be updated alongside torch
torchvision; platform_machine != 'aarch64' # Required for phi3v processor. See https://github.com/pytorch/vision?tab=readme-ov-file#installation for corresponding version
xformers; platform_system == 'Linux' and platform_machine == 'x86_64'  # Requires PyTorch 2.5.1
